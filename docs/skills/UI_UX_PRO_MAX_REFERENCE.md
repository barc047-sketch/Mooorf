# UI/UX Pro Max Reference

Optional reference: https://github.com/nextlevelbuilder/ui-ux-pro-max-skill

It was not installed during Codex setup because project-local discovery and the external installer path were not sufficiently explicit in the installed CLI. Claude or Codex may evaluate installation later when the environment and scope are clear.

Until then, use the lightweight checked-in `zonuert-visual-polish` workflow. UI/UX Pro Max may guide visual decisions only; it must not rewrite the store, graph schema, central graph, table sync, import/export contracts, or project model.
