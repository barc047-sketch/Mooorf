# Active Task

## R3B REAL ZOOM MODE + VISIBLE TOGGLE — WAITING OWNER QA

Status: LOCAL UNCOMMITTED / PREVIEW AVAILABLE / WAITING OWNER QA

- branch: `work/next-feature`;
- R3A Owner QA passed and is protected by local-only checkpoint `5f5e178e4de36c8ab8b1fa03d5b014b6e0b47878`; it was not pushed;
- R3B remains dirty and uncommitted for Owner QA;
- the existing registry-owned `ConnectionStudioWidget` remains the one universal Style Panel for Relationship Type, single-Connection and fixed multi-Connection targets; Custom Connections use the same override path;
- one extensible stroke registry now owns the six existing patterns plus the bounded advanced set: Zigzag, Wave, Scallop, Vertical Hash, Vertical Hatch and Lightning; each entry declares its preview and Canvas renderer strategy without creating a second geometry owner;
- canonical `butt | square | round` caps and `miter | bevel | round` joins use icon-first controls, width authors safely from `0.5–64`, Dash scale is now shared Pattern scale, and capability-gated Pattern amplitude appears only for applicable procedural motifs;
- procedural rendering samples the unchanged canonical Connection path as its centerline; endpoints, anchors, lane geometry, hit testing and persisted topology remain unchanged, and no sampled point data is stored;
- the same registry/motif treatment feeds adaptive Manager, Quick Rail, Inspector and Style Panel previews; preview width adds repetitions while wavelength, tooth/hash size, amplitude, marker size and stroke width remain unstretched;
- one shared ephemeral store draft feeds the panel, batched Canvas resolver, Manager, pickers and Inspector previews without mutating canonical Connections, settings, Saved Views, exports or history before Apply;
- single and multi editing display resolved Mixed values without inventing a shared style; only touched fields preview/apply, while untouched resolved values and local override differences remain exact per Connection;
- Apply creates one canonical history transaction for the fixed target set, Cancel creates zero and restores canonical resolution exactly, and Reset removes selected local visual overrides in one transaction;
- project type defaults and project-level factory style overrides reuse their existing settings/history owners; live drafts reach inheritors while explicit local values remain overriding;
- compact preview surfaces adapt within approximately `105–165px`; Manager, Inspector and Style Panel surfaces adapt within approximately `145–240px`;
- explicit type choice in the single Inspector, multi Inspector, or selected-set Quick Rail atomically reclassifies the target set and clears local styling overrides while preserving anchors, endpoints, enabled state, annotations and unrelated semantic fields; Undo restores each exact prior type/visual pair, while Manager archive/delete reassignment remains a non-destructive maintenance migration;
- Vertical Hash remains base-plus-perpendicular-marks; Vertical Hatch uses the same bounded procedural grammar but draws marks only, retaining an invisible canonical path for markers, placement and hit testing;
- adaptive preview length remains automatic layout behavior and Pattern Scale/Amplitude remain the authored spacing/length controls;
- Relationship Manager, single Inspector and multi Inspector open the same panel; the Manager `CONNECTIONS` tab remains a placeholder;
- the batched Canvas renderer consumes the shared transient resolver; selected Connections retain the exact resolved authored/transient stroke instead of receiving a thicker replacement redraw, while endpoint selection rings and focus opacity `1.00/0.76/0.44` remain;
- the final Canvas draw boundary now measures active 2D transform scale together with the CSS/backing-store ratio; camera zoom remains embedded in projected path geometry, so Fixed on Screen divides visual draw metrics by the measured output scale while Scale with Canvas additionally applies bounded camera zoom;
- width, dashes, procedural wavelength/amplitude, hash/hatch metrics, markers and bounded selection expansion share that final compensation at `0.25×`, `1×` and `4×`; the fixed 12px hit corridor remains screen-space practical in both modes and no compensated value enters canonical style data;
- the existing Relationship Manager header tune action now opens a bounded `CONNECTION SETTINGS` surface with one visible icon-first `VISUAL SCALE` choice: `Fixed on Screen` or `Scale with Canvas`;
- canonical `settings.connectionView.visualScaleMode` owns the project-global choice, defaults missing/legacy data to `screen`, updates mounted Canvas base/hover passes immediately without Apply/history, and reuses existing project/config/Saved View/recovery normalization and serialization;
- single, multi and Relationship Type live drafts use the same Canvas metrics resolver; adaptive UI specimens remain camera-independent;
- Enter inside the active Style Panel uses the existing Apply command after eligible slider/single-line values finalize; open menus, native buttons/options, multiline/contenteditable surfaces, already-handled events, repeats, modifiers and IME composition retain ownership, and the Apply button shows a quiet `↵` hint;
- annotation appearance is visibly deferred until the later canonical schema; no Canvas annotation was added;
- affected final-render/settings/keyboard contracts: 88/88 passed; TypeScript, tracked/untracked whitespace checks and scope review passed; the preview returned HTTP 200 at `http://127.0.0.1:5173/`;
- R5 keeps explicit ownership of advanced port layouts (Auto/Center, Cardinal 4, Horizontal 2, Vertical 2) and the other deferred Connection settings; only Visual Scale is authorized early here;
- no subagents, reviewer agents, browser automation, production build, CONNECTIONS-tab implementation, advanced ports, export, Matrix, Table or Classic work occurred;
- no R3B commit, push or merge occurred; `main` is untouched.

Next safe action:

Owner visual and interaction QA of both Visual Scale modes, the Relationship Manager settings surface, live Style Panel drafts and preserved Enter Apply behavior. Finalization, push, merge and branch cleanup each require separate explicit authority.
