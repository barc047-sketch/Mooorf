# Project Ledger

Only durable current truth belongs here. Closed history stays in linked legacy reports unless it explains a present constraint.

## Owner decisions

- **DEC-001 — Organism is the primary visible renderer.** Classic is retained internally for compatibility and failure recovery, not exposed as the normal product choice.
- **DEC-002 — Master Graph owns project meaning.** Canvas, Table, Dashboard, analysis, and export are projections; UI tools and registries never become parallel product stores.
- **DEC-003 — Automatic performance is runtime-only.** It may reduce only the live Membrane target, motion cost, and Cell Shadow cost. Text, overlays, interaction geometry, and authored settings remain unchanged.
- **DEC-004 — Authored exports use full quality.** Export reads a stable authored snapshot and does not wait on or mutate live preview state.
- **DEC-005 — Owner performs visual QA.** Codex supplies focused contracts, typecheck, diff/console sanity, preview, and limitations; only Owner PASS opens finalization.
- **DEC-006 — Magnet remains disabled until M3C.** Metadata and disabled ToolIds do not constitute a snapping feature.
- **DEC-007 — Master Graph owns future relationships.** Semantic Relationship, Visual Connection, and Membrane Bridge remain separate concepts and owners.
- **DEC-008 — No automatic main merges.** Commit/push and merge are distinct gates; merge requires an explicit Owner command.
- **DEC-009 — One owner per surface.** Reuse the existing Inspector, Runtime Status, Quick Controls, WidgetHost/Frame, file-intake provider, export service, and history path.
- **DEC-010 — Preview is not product data.** Appearance, Arrangement, resource, Membrane, and performance previews remain ephemeral and absent from persistence/history until an explicit apply action.
- **DEC-015 — Organism is the sole production renderer.** Classic remains legacy compile-only code and is outside production visual QA, performance and export parity. Organism visual output is PNG, PDF and presentation ZIP; CSV/JSON remain data outputs; SVG stays an explicit legacy limitation.

## Recently resolved

- **CONNECTIONS R6 EXPORT PARITY + TABLE/MATRIX PROJECTION HOOKS — LOCAL UNCOMMITTED / WAITING OWNER QA.** From clean remote-backed R5.5 checkpoint `4154bd5dda986db7c8ec7d63c6df90b1ffd180d1`, one React/DOM-independent export projection now reuses canonical Relationship Type/style resolution, deterministic pair lanes, all four path geometries, all current dash/procedural families, markers and the shared annotation resolver/presentation projector. Detached export metrics use explicit document scale and never live camera compensation; export annotation mode retains authored visible Title/Body and full canonical Body text without interactive LOD/focus/collision budgets. Detached Organism PNG now draws this projection between the WebGL field and Cell presentation, so PDF and presentation ZIP inherit the same composition; the existing raster PDF sheet architecture and explicit Organism SVG limitation remain unchanged. An optional caller-owned x/y/width/height Legend target recomputes the existing pure Legend projection and renders resolved specimens without Widget geometry, DOM capture or stored item positions, forming the future Sheet seam. Pure Table rows and sparse Matrix cells retain canonical Connection references/IDs, dynamic Type names/codes, disabled state, override indicators and multiple records per pair; no UI was added. Presentation packs add semantic `relationships.csv`, while JSON remains canonical and renderer-free. RED was recorded, 106/106 focused/affected contracts and exact TypeScript passed, tracked/untracked checks passed, and `http://127.0.0.1:4173/` returned HTTP 200. No browser automation, production build, commit, push or merge occurred.

- **CONNECTIONS R5.5 FINAL EDITORIAL LEGEND SETTINGS UX PASS — LOCAL UNCOMMITTED / WAITING OWNER QA.** The earlier surface remained transparent because the generic Manager-section background overrode the intended glass treatment. `LEGEND SETTINGS` now applies the shared `.glass` primitive directly, retaining its actual fill, border, blur, highlight and theme behavior. Its compact icon-first controls use semantic `textWidth` (`80–320px`), `textAlign`, `textPlacementX` and `textPlacementY` alongside the accepted Legend configuration. Width enters the pure grid sizing/reflow calculation; alignment and placement remain output-only semantic values with no per-Type coordinates, phantom height or history. Visual specimen-length marks, numeric width field and content chips replace the prior text-button wall; existing Manager range controls remain on shared `SliderRow`/`org-slider`. Specimen-side is intentionally deferred because X placement already supplies bounded alternatives without duplicate layout semantics. Affected contracts passed 23/23; TypeScript, tracked/untracked whitespace checks and the current-worktree HTTP 200 preview check passed. No browser automation, production build, commit or push occurred.

- **CONNECTIONS R5.5 OWNER-QA LEGEND HEIGHT + SETTINGS VISUAL CONSISTENCY CORRECTION — LOCAL UNCOMMITTED / WAITING OWNER QA.** The actual premature resize constraint was the generic workspace `180px` minimum, applied by WidgetFrame workspace CSS before Legend composition could reflow; the empty output also retained that old content height. The frameless Legend now computes its resize floor from a zero-height current-width projection, density/content-dependent projected row height, actual border and tiny control rail clearance. It can therefore shrink after a prior no-scroll growth or after scope/content/density reduces, while large libraries still reflow and grow only within viewport bounds. `LEGEND SETTINGS` now reuses the accepted Morph-glass surface/tokens, and Hit tolerance/Unrelated fade reuse the shared `SliderRow`/`org-slider` instead of native range appearance. The accepted detached architecture, Type semantics, linetypes, export seam and Manager-independent lifecycle remain unchanged. Affected correction contracts passed 22/22; TypeScript, tracked/untracked whitespace checks and the current-worktree HTTP 200 preview check passed. No browser automation, production build, commit or push occurred.

- **CONNECTIONS R5.5 FINAL EDITORIAL LEGEND + LINETYPE REFINEMENT — LOCAL UNCOMMITTED / WAITING OWNER QA.** From Owner-accepted R5 checkpoint `833ca108b14bdaa474be39fead710136bf211ab5`, the independent Legend now removes its visible title/header and uses one invisible shell drag region plus tiny vertical minimize/close controls in the existing WidgetFrame. Its output is a no-scroll, specimen-first technical key with materially tighter density, dynamic Code/Description height and bounded content-driven window growth; Manager remains independent and owns all settings. Canonical semantic config adds Short/Standard/Long `specimenLength` and Legible/True `specimenWeight`; Legible raises only the preview minimum for thin lines and never mutates Type/Connection/Canvas/clipboard truth. Long Dash, Dash-Dot-Dot, Sparse Dot and Centerline are the only added base families, all driven by existing Pattern Scale rather than loose/dense duplicates. The pure projection, stable Type order, live closed-Manager updates, advanced motifs/markers, Vertical Hatch hidden centerline and future Sheet/export seam remain intact. Affected refinement contracts passed 36/36; exact TypeScript, tracked/untracked whitespace checks and the current-worktree HTTP 200 preview check passed. No browser automation, production build, R6 export, Sheet, commit or push occurred.

- **CONNECTIONS R5 WHOLE-CELL AUTHORING — OWNER ACCEPTED / LOCAL CHECKPOINT `833ca10`.** The Owner amendment removes visible V1 authoring ports and the `portLayout` project setting. `C` mode now reuses the complete existing visible Cell hit body for source and target acquisition; the one batched interaction overlay draws restrained source/valid-target outlines and never gives invalid Cells the valid treatment. Every new Connection persists canonical `auto` start/end anchors, overriding project Type side defaults without deleting historical side-anchor compatibility. The existing Manager tune surface retains Visual Scale, dynamic Default Type, Stay in mode, Select new, Edge auto-pan, existing-line hit tolerance, unrelated fade, Reduced/Standard motion and bounded reset. Custom defaults to a gentle curved `3px` solid at authored opacity `0.82`; factory/project Type styles and explicit `0.5–64px` widths remain independent. Focus defaults are `1.00/0.82/0.55`. Authoring retains one transaction, Inspector priority and one camera scheduler. Affected contracts passed 144/144; TypeScript and whitespace checks passed; `http://127.0.0.1:5173/` serves the current worktree. No browser automation, production build, push, export, Matrix/Table, Classic, dependency or renderer rewrite occurred.

- **CONNECTIONS R4B ANNOTATIONS + SHARED VISUAL SCALE — LOCAL UNCOMMITTED / WAITING OWNER QA.** Canonical sparse Title/Body and annotation presentation now resolve through Relationship Type defaults, Connection overrides and the universal Style Panel's one transient draft. Inspector owns per-Connection placement; one batched Canvas projection owns horizontal draw, deterministic collision handling and bounded projected-screen-space LOD. The existing project-global `settings.connectionView.visualScaleMode` is the sole scale owner for both Connection graphics and annotations: Fixed mode keeps the complete unit stable in final CSS pixels, while Canvas mode scales typography, plate, spacing, wrapping width and offset together through normalized zoom with one final output-scale compensation. Authored values remain zoom-independent. Focused affected contracts passed 73/73, TypeScript and whitespace checks passed; no browser automation, build, commit or push occurred.

- **CONNECTIONS R4A ENTER APPLY + CONNECTION MODE INSPECTOR CORRECTION — LOCAL UNCOMMITTED / WAITING OWNER QA.** Real-browser reproduction showed that a clicked icon-first Style control retained native button focus and the scoped policy therefore preserved Enter before the canonical Apply callback; a selected Cell likewise continued to outrank the already canonical `connectionModeActive` flag in Inspector presentation. Style options are now explicitly Studio-owned for plain Enter, pending field work settles before the unchanged Apply callback, and the shared Relationship Type listbox handles highlighted Enter selection without applying Studio. Inspector priority is selected Connection(s), Connection mode, then Cell, with a compact current-Type mode state and no new selection or authoring owner. Focused affected contracts passed 46/46. Browser QA verified persistence, one-step Undo, listbox select-only behavior, Cell suppression during endpoint authoring, selected-Connection priority and Cell restoration on exit. No production build, commit or push occurred.

- **CONNECTIONS R4A TYPE DUPLICATION + UNIVERSAL STYLE CLIPBOARD — LOCAL UNCOMMITTED / WAITING OWNER QA.** The accepted R4A Manager now exposes icon-first Duplicate, Copy Style, Paste Style and Edit Style actions for exactly one selected Relationship Type. A pure duplicate helper turns factory or Custom sources into ordinary project types with unique stable ID/name/code, copied safe defaults/annotation/style, zero usage and no Connection records. Type Copy and Paste reuse the existing source-agnostic `connectionStyleClipboard`; Paste updates project Type defaults or the project-level factory override layer in one history transaction, immediately updating inheritors while Connection-local overrides remain authoritative. Native editable Cmd/Ctrl+C/V precedence remains intact. Focused Manager, Relationship Type, style, history and shared-selection contracts passed 63/63, TypeScript and `git diff --check` passed. No browser automation, production build, commit or push occurred.

- **CONNECTIONS R4A CONNECTIONS TAB — LOCAL UNCOMMITTED / WAITING OWNER QA.** On the pushed accepted R3B checkpoint `e9f9219524e2f4721670151340456a7e92bfab77`, the existing registry-owned Relationship Manager now projects canonical Connections in stable order with reusable pure query/type/Active/Inactive/override filters, endpoint/type/code/title/body search, compact resolved style previews, enabled and sparse visual/annotation indicators, explicit empty/reset states, shared row/Shift/checkbox/select-all selection and a fixed-row overscan window for large lists. Bulk Change Type and Delete reuse existing atomic supersession/deletion commands. Locate selects the canonical record, returns Table to Canvas and fits the existing camera around endpoint Cells without history or stored geometry; global Connections visibility remains distinct from enabled state. The Owner-approved desktop interaction amendment adds one UI-only visible-order range anchor, forward/reverse filtered Shift ranges, Cmd/Ctrl toggles and guarded visible Cmd/Ctrl+A while Canvas remains additive/toggle. Focused R4A/Manager/shared-selection/lifecycle contracts passed 38/38, TypeScript and `git diff --check` passed. No browser automation, production build, commit or R4A push occurred; Owner visual/interaction QA remains the gate.

- **CONNECTIONS R3B REAL ZOOM MODE + VISIBLE TOGGLE — LOCAL UNCOMMITTED / WAITING OWNER QA.** Owner QA established that resolver-only proof did not reach the final Canvas boundary. The live renderer now measures active 2D transform scale against the Canvas CSS/backing-store ratio and compensates every stroke, dash, procedural motif, marker and bounded selection metric there; camera zoom remains in projected path geometry. The canonical project-global `settings.connectionView.visualScaleMode` defaults legacy/missing data to Fixed on Screen, while Scale with Canvas deliberately applies bounded camera zoom. Relationship Manager tune/settings exposes the two icon-first choices and updates both base and hover passes immediately without Apply or history; existing project/config/Saved View/recovery normalization persists the mode. Hit tolerance stays screen-space practical, Vertical Hatch remains marks-only, live Style drafts follow the active mode and Enter Apply is unchanged. Affected contracts passed 88/88, TypeScript and whitespace checks passed, and `http://127.0.0.1:5173/` returned HTTP 200. No subagents, browser automation, production build, advanced ports, broader R5 UI, export, commit, push or merge occurred.

- **CONNECTIONS R3B SCREEN-SPACE STYLE + ENTER APPLY — LOCAL UNCOMMITTED / WAITING OWNER QA.** The batched Organism Connection renderer now routes visual metrics through one pure internal `screen | canvas` seam that defaults to `screen` and normalizes camera zoom to the existing `0.25–4` bounds. Camera-projected path positions/extents continue zooming, while authored width, dash/gap values, procedural wavelength/amplitude, Vertical Hash/Hatch dimensions, marker size/offset, bounded selection expansion and the practical hit corridor remain screen-stable; canonical styles and camera-independent UI specimens are unchanged. Single, multi and Relationship Type live drafts share the same Canvas resolution. Style Panel Enter invokes the same Apply command after pending range or valid single-line values finalize, with no duplicate transaction; menus/options, native buttons, multiline/contenteditable surfaces, prevented/repeated/modified events and IME composition retain ownership. Apply shows a quiet `↵` hint. Affected contracts passed 58/58, TypeScript and whitespace checks passed, and `http://127.0.0.1:5173/` returned HTTP 200. No subagents, browser automation, production build, R4/R5 UI, export, commit, push or merge occurred.

- **CONNECTIONS R3B TYPE SUPERSESSION + HATCH + SELECTION CORRECTIONS — LOCAL UNCOMMITTED / WAITING OWNER QA.** Explicit type choice from the single Inspector, multi Inspector or selected-set Quick Rail now clears sparse local styling in the same atomic reclassification transaction while retaining anchors, endpoints, enabled state, annotations and unrelated semantic fields; Undo/Redo restores each exact prior type/visual pair. Relationship Manager archive/delete reassignment remains a maintenance migration and preserves bespoke visuals. Vertical Hash remains base-plus-marks; new Vertical Hatch draws perpendicular marks only while its invisible canonical path retains endpoint markers, motif placement and hit testing. Adaptive previews remain automatic, Pattern Scale/Amplitude retain authored spacing/length ownership, and selected Connections keep the exact resolved authored/transient stroke without a thicker replacement overlay or procedural base-line reveal. Focus opacity remains `1.00/0.76/0.44`. Affected contracts passed 67/67, TypeScript and whitespace checks passed, and `http://127.0.0.1:5173/` returned HTTP 200. No subagents, browser automation, production build, commit, push or merge occurred.

- **CONNECTIONS R3B ADVANCED STROKE CORRECTION — LOCAL UNCOMMITTED / WAITING OWNER QA.** The single universal Style Panel now exposes canonical icon-first cap and join controls, a `0.5–64` width range, shared Pattern scale and capability-gated Pattern amplitude. One extensible registry preserves Solid, Dashed, Dotted, Dash-dot, Double and Segmented while adding bounded Zigzag, Wave, Scallop, Vertical Hash and Lightning treatments with shared preview/Canvas strategies. Procedural motifs sample the unchanged canonical path as their centerline; no endpoints, anchors, lane geometry, hit testing or persisted sampled points change. Shared previews adapt within `105–165px` compact and `145–240px` standard bounds; greater length adds motif repetitions without stretching motif metrics. Live type/single/multi preview, touched-field Apply/Cancel behavior, clipboard parity and focus opacity `1.00/0.76/0.44` are preserved. Focused and affected contracts passed 79/79, TypeScript and whitespace checks passed, and the existing `http://127.0.0.1:5173/` preview returned HTTP 200. The later R5 Owner amendment supersedes the then-planned advanced visible-port layouts with whole-Cell authoring. No subagents, browser automation, production build, commit, push or merge occurred.

- **CONNECTIONS R3B OWNER-QA LIVE/MULTI CORRECTION — LOCAL UNCOMMITTED / WAITING OWNER QA.** The one registry-owned Style Panel now uses canonical geometry, stroke and directional marker specimens; its shared runtime-only draft resolves live on the batched Canvas and in Manager, picker, Inspector and panel previews without canonical/history writes. Single and fixed multi-Connection targets use the same panel: Mixed is presentation-only, untouched local differences remain exact, touched fields sparsify independently, Apply is one history entry total and Cancel is exact/history-free. Relationship Type/factory drafts preview through inheritors while local overrides still win. Shared previews are intrinsic 132px specimens. Quick Rail keeps no-selection authoring at zero Connection history and, with selected Connections, atomically reclassifies the set in one history entry while preserving overrides/annotations/identity, setting the current authoring type and recording MRU once. Focus opacity remains `1.00/0.76/0.44`. Focused and affected contracts passed 93/93, TypeScript and whitespace checks passed, and `http://127.0.0.1:5173/` returned HTTP 200. No browser automation, production build, Canvas annotation, CONNECTIONS tab, ports, export, Matrix, Table, Classic, commit or push occurred.

- **CONNECTIONS R3B — LOCAL UNCOMMITTED / WAITING OWNER QA.** On `work/next-feature` after R3A local checkpoint `5f5e178e4de36c8ab8b1fa03d5b014b6e0b47878`, the existing registry-owned `ConnectionStudioWidget` is the one universal Style Panel for Relationship Type defaults and single-Connection overrides; Custom Connections use the same override path. The fixed target and resolved draft are ephemeral, panel-local and history-free until Apply. One Apply enters one existing history owner: project Relationship Types replace their full canonical visual defaults, factory types update the safe project override layer, and Connections rebase the draft into minimal sparse `visual` differences. Cancel creates zero history; Reset removes the local override in one transaction without touching semantics, endpoints, Title or Body. Geometry, six canonical patterns, width, opacity, color, markers, curve, dash scale, marker size and marker offset use the current registries and bounds. Manager, picker, Inspector and panel previews reuse `RelationshipTypeStylePreview`; only Manager rows and the single-Connection Inspector launch style editing. The batched Canvas projection now supplies project Relationship Types to the existing resolver so inherited project defaults render, while explicit local fields, focus opacity `1.00/0.76/0.44`, OFF behavior, cache architecture and pointer ownership remain unchanged. Annotation appearance is visibly deferred; no Canvas annotation, CONNECTIONS tab, multi-style editing, ports, export, Matrix, Table, Classic, browser automation or production build occurred. Focused and affected contracts passed 74/74; TypeScript, tracked/untracked whitespace checks and scope review passed; `http://127.0.0.1:5173/` returned HTTP 200. R3B remains uncommitted and nothing was pushed; Owner visual/interaction QA is the active gate.

- **CONNECTIONS R3A — OWNER QA PASSED / LOCAL CHECKPOINT.** From feature base `120058e2f7cdffd7dd5080df1e42cce8c666fe2b`, the existing registry-owned `connections` widget is the single large `RELATIONSHIP MANAGER` using workspace geometry at `40vw × 78vh`, bounded to `44vw × 85vh` and viewport margins. R3A implements only `TYPES`; `CONNECTIONS` remains a next-stage placeholder. The Manager consumes the stable canonical factory/project library, cached usage index and resolved visual defaults. Owner-approved Amendment 2 adds one shared 240px authoring picker with approximately five internally scrolling rows, the same canonical preview primitive for Manager and picker rows, and a ten-ID per-user local MRU that updates only after actual Quick Rail/single Inspector/bulk Inspector/Manager reassignment use. MRU remains outside project/Saved View semantics and Connection history and never reorders Manager rows; unavailable IDs are ignored. Multi-Connection Inspector shows shared Type or presentation-only `Mixed`; one bulk Type assignment creates one history transaction and preserves each Connection's visual, annotation, endpoints, anchors, enabled state and unrelated semantics. Single and multi Delete remain canonical record removal with atomic Undo. Initial R3A evidence passed 60/60; correction evidence passed 63/63 affected contracts followed by TypeScript, tracked/untracked whitespace checks and an HTTP 200 preview response at `http://127.0.0.1:5173/`. Owner QA passed, and the exact R3A scope is protected by local-only checkpoint `5f5e178e4de36c8ab8b1fa03d5b014b6e0b47878`; it was not pushed or merged.

- **CONNECTIONS R2B.2 — LOCAL UNCOMMITTED / FINAL VISUAL CORRECTIONS APPLIED / WAITING OWNER QA.** On `work/next-feature` after the accepted R2B.1B foundation `6c41d25381ad7943f6b2fa3938520864bfdbbea8` was normally pushed, the compact selected-Connection Inspector retains dynamic factory/project Relationship Types, sparse Title/Body controls, resolved style preview, Reverse and canonical Delete. The repository-loaded active project has no project-created types; fixtures prove active project types appear, archived types do not, and Custom remains safe. Existing click/Shift-click selection is history-free; a distinct multi-Inspector state no longer presents one selected record. Guarded Delete/Backspace removes the selected canonical set atomically. The Owner-approved transient style clipboard captures one source's resolved geometry, pattern, markers and appearance scalars, excludes identity/meaning/annotations/visibility/anchors, and rebases minimal sparse overrides across one or many targets in one Undo/Redo transaction. Final Owner-QA visual correction changes only the batched renderer focus presentation: selected `1.00`, endpoint-related `0.76`, and contextual `0.44`; selected-Cell focus retains the contextual floor. One accessible custom glass `RelationshipTypePicker` serves both surfaces without changing the approved Quick Rail upward direction; the Inspector opens it downward. Focused presentation/Inspector contracts passed 40/40, TypeScript and diff checks passed, and `http://127.0.0.1:5173/` returned HTTP 200. No browser automation, production build, Relationship Manager, Style Panel, Canvas annotation, port, renderer rewrite, export, Matrix, Table or Classic work occurred. R3, finalization and merge remain unauthorized.

- **CONNECTIONS P2 — PUSHED / WAITING OWNER REVIEW.** On `work/next-feature`, product commit `be1b9748298c35a3c89736afaf0ede5e533806ee` adds exactly one Connections Dock launcher and registry-owned widget, a pure source/target authoring reducer, explicit ephemeral Connection selection, a bounded existing-Connection selection list, and semantic Connection editing inside the one existing Inspector. The only Canvas visual is one non-persistent SVG creation-preview line using Organism-local/ref pointer state, existing hit testing and camera feedback; it creates no pointer-time store/history writes and disappears on terminal actions, Escape, Table activation or widget close. Exact duplicates select the existing record without history, distinct semantics for the same pair remain allowed, and completed commands reuse existing one-transaction history. Final evidence passed 46/46 focused and affected contracts, TypeScript, tracked/untracked diff checks and Codex browser QA at 1440×900 and 1280×800 with zero console errors. No production build, stored-line renderer, visual grammar, export rendering, Matrix, Material, Morph Bridge, Behaviour, animation or Classic work occurred. Human smoothness/visual judgement remains Owner review; Prompt 3 and merge remain unauthorized.

- **CONNECTIONS P1 — PUSHED / WAITING OWNER REVIEW.** From synchronized source `3f62032c54d76a014a781504cc5cd8e4b5ee63d9`, product commit `01a6c916b426b63628b1a8d0d94c8fb530bfc6c8` establishes the canonical Master Graph/central-store `connections` collection, six-type semantic registry, stable modifiers and future-expandable visual IDs, safe unknown-ID fallback, cached endpoint/pair/exact-semantic indexes, one-transaction commands/history, and atomic dependent cleanup with Cell Undo/Redo. Project JSON, `.mooorf`, Saved Views, recovery and applicable schedule transfer retain or safely prune Connections; old projects receive `[]`. The durable performance contract defines the eight-stage roadmap and prohibits per-Connection React components, permanent static loops, per-frame store/history writes, persisted path geometry and unchecked all-to-all work. Final evidence passed 58/58 focused and affected contracts, TypeScript, tracked/untracked diff checks and scope review. No UI, renderer, Morph Bridge, Behaviour, Matrix, Material, animation, Classic, visual export or production build was implemented. Prompt 2 and merge remain unauthorized.

- **ORGANISM CELL LABELS & CALLOUTS — COMPLETE / MERGED.** Owner verdict: PASS. Fast-forward merged into `main` at integrated product commit `a7ef4cce4ff0ca4def8e5f5dcb74c5077369ce2e`; Label Studio uses the approved Lucide `MessageSquareText` callout icon, with direct Label Studio/Membrane launchers and persistent selected-state feedback included. Focused contract passed using the established `tsx --test` harness; TypeScript and diff check passed. The requested Vitest wrapper reported no Vitest-owned suite after the contract script passed, which did not invalidate the repository contract. This completed milestone is the source lineage for the later Connections P1 branch work recorded above.

- **CELL-LABEL RUN 2 — Owner-QA Correction 2.** On `work/next-feature`, beginning at `9366d9d25a9cfb5d89cc72af13664fd1a85b50eb`, correction commits `199eda0afaa1f10cce43de57ebeca3466611f22a`, `96ac4e38303d79bd1e9e84b4f97c16a2729819b5`, `b4e7a3ec04c8a6e14625cd241cbba23dc32e5a67`, and `61fcb9d313b91d486d2287156d77e84eb972658a` expand the shared Organism label resolver from nine to twelve production layouts, adding Dual Ring, Ring + Core and Technical Orbit. The pre-correction Owner-QA record was: Flag lost stable, configurable callout behaviour; Ring changed to straight fallback blocks while zooming out; canonical Body could not occupy a Ring arc; inside text escaped small Cells; Membrane geometry distorted with camera zoom; advanced controls had no usable home; Display lacked canonical text/camera controls; and no optional drag feedback existed. Ring now retains curved `ring` identity at every zoom with independently configured primary/secondary arcs, including canonical Body text; shared runtime fitting bounds inside labels without zoom-time product writes. Flag now has deterministic advanced placement, leader, panel and content configuration while remaining presentation-only and relationship-free. The compact Inspector and WidgetHost-based Label Studio share the same sparse Project Default/local override/history owner. Display reuses canonical camera and label-fit settings; Membrane zoom controls affect sampling only, and camera shake is runtime-only. PNG/PDF/presentation-ZIP retain the shared detached Organism projection; CSV/JSON remain intact and Organism SVG remains unavailable. Focused correction contracts, TypeScript, final diff check and the one authorized build passed. Status: WAITING OWNER QA; no merge, Classic work or later phase is authorized. Codex browser checks at 1440×900 and 1280×800 covered the core control/geometry paths; manual drag, reduced-motion/Table-shake and downloaded-artifact comparison remain Owner/manual verification.

- **CELL-LABEL RUN 2 — Organism-only nine-layout finalization.** On branch `work/next-feature` from remote base `2c81c00fb4ef19acbb911acdd9ac402814ecb1da`, Fable’s four commits through `52293c4dff1fc9be05561935337e9846da1d4ce6` were preserved and Sol product commit `e18b2dd5b23a0610a570f9c5322630534cf379d6` finalized Centre Stack, Area Hero, Name Hero, Split, Symbol + Text, Compact Technical, Ring Label, Minimal Number and Flag through one label contract/resolver. Ring now shares deterministic arc/tracking/font/ellipsis/fallback decisions across live Organism and detached export; Flag remains presentation-only with deterministic direction and bounded geometry. Project Default, sparse Cell overrides, Mixed state, one history transaction, project files and Saved Views reuse existing owners. PNG/PDF/ZIP draw circles, symbols and labels directly from the authored Organism snapshot; CSV/JSON remain intact and Organism SVG is unavailable. Ordered focused suites, TypeScript, diff checks, 1440×900 and 1280×800 browser QA, real artifact generation, `.mooorf`/Saved View round trip, three mounted Canvas/Table cycles, and 20/51/101/251/501 authored-space probes passed with the existing 96-nucleus render cap. The sole build transformed 2,928 modules in 45.23s with only the accepted chunk-size warning. Classic was not audited or developed. Status: WAITING OWNER QA; no merge or later phase is authorized.

- **PF1D.3 — Table file workflow.** On source SHA `9011ff2ba56fbda3831b4405b0960c0ddb3075ea` and branch `feature/pf1d-lazy-workspaces`, Owner QA passed the approved 35/65 Table workspace, 70/30 Upload/Download cards, and full-width Search. The Table accepts CSV, XLS, and XLSX schedules; its XLSX template contains `SPACES` and `README`; inline review exposes a multirow scrollable preview plus validation and diagnostics. Add and Replace reuse the central-store import boundary and retain Canvas/store synchronization. Automated evidence passed 29/29 with zero skips, including 500-row import and bounded virtualization compatibility; TypeScript and tracked/untracked diff checks passed. The one authorized build exited 0, transformed 2,899 modules, emitted `TableView-DG_Zb1Rp.js` at 59.32 kB / 20.19 kB gzip, and retained only the accepted Vite >500 kB warning. Owner verdict: PASS. Product commit `6293831ebe63b0f7a2d6f0027f9c14b15b03549a`; this documentation closeout is pushed to the feature branch. No Canvas or Classic files changed, no PR exists, and no merge is authorized. Next work is discussion/mapping for Text Layouts, Materials, and Connections only; there is no automatic next implementation.

- **PF1D.2 — Organism pause lifecycle, instant Table shell, and bounded rows.** On source SHA `a3ed336fb0e72fbdb8d8a513a27487c3cb0f73ca` and branch `feature/pf1d-lazy-workspaces`, Table activation pauses the existing Organism frame scheduler and all WebGL/2D drawing; inactive invalidations collapse, and Canvas return prepares one current-state/current-theme frame before a generation-safe App readiness gate releases the static scrim. `src/App.tsx` owns the sole 400ms fallback; stale readiness cannot release a later generation. Classic is unchanged and receives no PF1D.2 activity/readiness props. Table publishes shell-ready independently from rows and uses one fixed-row virtual window with canonical store-backed editing; this basic windowing was accelerated from PF1D.3. Owner browser QA passed at 30/300/500 rows: shell at 53.2/65.6/68.2ms, bounded mounted rows, reachable last row, edit-to-Canvas synchronization, one deduplicated lazy request, 12 clean rapid cycles, and zero Organism draws in a 750ms Table probe. Three repaired PF1D.1 source contracts passed 3/3; the final suite passed 28/28 with zero skips; TypeScript and diff checks passed. The sole build exited 0, transformed 2,898 modules, emitted `TableView-IzkUI7Jx.js` at 48.35 kB / 16.54 kB gzip, and retained the known non-blocking >500 kB Vite warning. Owner verdict: PASS. Product commit `ee58458282ffb6446ff1c74c2483fb392a5bf55a`; Project Engine closeout: this documentation commit; both pushed to `origin/feature/pf1d-lazy-workspaces`; no merge.

- **PF1D.1E — Lazy floating Table workspace overlay.** On source SHA `019ca3de55c93bf5f8de7f57e666eeaeddaab7ed` and branch `feature/pf1d-lazy-workspaces`, `src/App.tsx`, `src/App.css`, and `src/runtime/pf1dContracts.test.ts` establish one persistent Canvas and one lazy conditional Motion Table under a direct-child isolated `.app-shell` stack: Canvas 0, widgets 40, Rail/Dock/auxiliary 60, Table 90, ViewToggle 100. The static theme-aware scrim contains no live blur. Browser samples proved panel opacity/transform changed from mount through 60ms to 200ms; Rail/Dock were intermediate at 80ms and settled at 320ms; `elementFromPoint` over a moved widget resolved inside `.table-workspace-panel`; the return scrim protected restoring chrome; moved and minimized widget geometry remained exact. Canvas start requested 0 Table modules, first Table open 1, and repeat switches 0 additional. Focused suites passed 78/78 and TypeScript passed. The single production build exited 0, transformed 2,898 modules, and emitted lazy `TableView-Bet7Cgpb.js` at 45.46 kB / 15.63 kB gzip with the non-blocking Vite warning for chunks above 500 kB. Owner verdict: PASS. Product commit `32e3a798fe0a78386bc573b0e11497cc65ea025c`; Project Engine closeout: this documentation commit; both pushed to `origin/feature/pf1d-lazy-workspaces`; no merge.

- **E1 — Organism Detached Export Overlay Alignment.** Pre-existing P1 export blocker discovered during R0.3 Owner QA. Diagnostic evidence retained: neutral settings resolve at `dx=0`, `dy=0`, `dr=0`; X/Y offsets reached `dx=43.52px`, `dy=27.20px`; global/radial/angular displacement reached about `54.92px` X and `85.92px` Y; size variation diverged; 1×/2× changes density, not logical placement. Root cause: detached Membrane used resolved `ProductionNucleus` geometry while the overlay used raw `SpaceCell` geometry. Correction: `organismExport.ts` passes resolved `sx`, `sy`, `screenR` only to the detached overlay in `svgExport.ts`; the neutral/raw Classic fallback remains intact. Cell, Boundary, Core, Void, labels, and symbols now share the resolved geometry. Non-neutral transform, size-variation, 1×/2× logical-placement, and Owner visual QA passed; no durable `SpaceCell` mutation. Product commit `beb5476164d171d25e4d61efef3e370b9bb1574b`; Owner PASS; Classic behaviour preserved.

- **BUG-001 — Day/Night background always black.** Resolved by PF1C: the Organism host owns the canonical background beneath the transparent surface.
- **BUG-002 — Membrane invisible.** Resolved by PF1C: transparent target presentation and invalidation retain a visible Membrane.
- **BUG-003 — Quick Controls shift with Inspector changes.** Resolved by PF1C: the shared fixed top-right anchor has no Inspector coupling.
- **R0.1 — Shared Canvas Gesture Transaction Core.** Renderer-neutral transaction ownership now lives in `src/interaction/canvasGestureController.ts`; Classic and Organism retain local hit testing, coordinates, invalidation, and render-loop ownership. A pre-finalization pan regression from a mutable camera anchor was corrected with immutable pan anchors and one zoom conversion. No schema, public store API, or persistence contract changed.
- **R0.2 — Behaviour-first parity and contract split.** Behavioural, wiring, and style contracts are explicitly separated. One redundant source-string download-delay assertion was replaced with executable proof; stale mounted-capture wiring now correctly names detached-export ownership; authored live/export characterization protects presentation, Membrane, Cell Shadow, nuclei projection, and runtime-state exclusion. No production API or behaviour changed.
- **R0.3 — Split Widget-owned CSS from Edge Shell.** Eleven semantic blocks and 95 selector clauses moved from `shell.css` to `widgets.css` with declaration/value/specificity/cascade parity exact. Shared Dock/widget range ownership remains in `shell.css`; Owner visual QA passed. No production TypeScript or React changed.

## Known limits

- **LIM-001 — Organism visible-nucleus cap is 96.** Larger-scale rendering needs a future texture/data-buffer architecture; current performance work does not claim 200+ Cells.
- **LIM-002 — Saved Views are local-first and capped at 20.** Project files preserve them, but no cloud/account ownership exists.
- **LIM-003 — Organism SVG is unavailable.** The current SVG route is Classic-oriented legacy code; PNG, PDF and presentation ZIP are the production Organism visual outputs.
- **LIM-004 — Grid presets are mostly metadata.** Dotted/None are the only live paths; snapping/export behavior for other presets is not implemented.
- **LIM-005 — Connections have a bounded batched Organism visual renderer and whole-Cell authoring but no visual export.** Canonical meaning, history, persistence, authoring, selection, Inspector editing, final screen compensation and Connection Settings are live. V1 exposes no visible authoring ports; advanced side-anchor UI is deferred. Export rendering, Matrix, Material, Morph Bridge and Behaviour remain separately gated work.
- **LIM-006 — Reference images, calibration, and rulers are unimplemented.** No canonical state, renderer layer, or preprocessing pipeline exists.
- **LIM-007 — Animation export is unimplemented.** Current export is static/presentation-pack only; GIF/WebM/MP4 require future queue/worker ownership.
- **LIM-008 — Dense labels remain crowded around 60+ spaces.** Bounded degradation hides Body/metadata and preserves Name/Area, but it does not claim collision-free labels above the 96-nucleus render cap.
- **LIM-009 — The known Vite main-chunk warning is accepted evidence, not a completed lazy-loading solution.** PF1D owns lazy workspace work.
- **LIM-010 — PF1D.2 basic row windowing is not the complete PF1D.3 dense-table workflow.** Progressive materialization beyond the current fixed-row window, expanded text-first editing, search, numeric area queries, sorting, schedule upload/template download, and inline import review remain separately gated.

## Superseded decisions

- **DEC-011 — Visible ORG/CLS switching is superseded.** One visible Organism strategy is normal; Classic remains internal fallback.
- **DEC-012 — Partial selection orbit/halo feedback is superseded.** Selection is now renderer-neutral external keyline feedback and never Organism geometry or export content.
- **DEC-013 — Full-resolution live Membrane at all times is superseded.** Runtime preview may downscale only the Membrane target while presentation overlays and exports remain full resolution.
- **DEC-014 — Starting every task from the full historical documentation stack is superseded.** The Project Engine read order is mandatory; legacy evidence is subject-routed only.
