# Codex Launch Prompt — C0 M2

Execute authorized milestone `C0 M2` for repository `barc047-sketch/Mooorf`.

Read and obey the complete contract first:

`docs/worker-briefs/C0_M2_CODEX_ADVANCED_INSPECTOR_SYMBOLS_RUNTIME_GATES.md`

from:

`origin/docs/mooorf-ai-team-operating-protocol`

Critical rules:

1. Work only on:
   `feature/c0-m2-advanced-inspector-symbols-runtime-gates`

2. Verify exact base:
   `main@bd160b8c615cdafbbdd8a76332be46b69a6e765e`

3. Update `status/codex` to `RUNNING` before production edits.

4. Preserve exactly one Inspector with:
   `Content | Appearance | Symbol`

5. Implement the bounded M2 scope:
   - advanced Cell/Boundary/Core controls;
   - canonical Cell Shadow with Shadow Strength and hard Off gate;
   - Membrane Field advanced controls including Field Edge Softness;
   - independent Membrane Edge Softness only if it has separate state, renderer effect and export fallback;
   - preserve Cell Gradient and Solid Membrane modes;
   - runtime power gates for Membrane, Edge, Shadow, Motion, Labels, Grid and inactive Snapping work;
   - production Symbol tab with audited baseline, verified Lucide additions and aliases;
   - search, categories, recents, favourites, hover preview/revert, apply/replace/remove;
   - symbol placement/backing, history, persistence and export.

6. Do not implement custom architectural vectors, Quick Bar, snapping UI/engine, Material Browser, Connections, Annotation, broad Table redesign or M3/M4 work.

7. Obey Fast Owner-QA:
   - focused executable contracts;
   - affected history/persistence/migration/renderer/export tests;
   - typecheck;
   - exact diff check;
   - exactly one final production build;
   - no exhaustive CDP matrix;
   - stop QA-harness tuning after 10 minutes;
   - provide a 3–7 item Owner yes/no checklist.

8. Push one fixed feature head, update `status/codex` to `WAITING_REVIEW`, and stop.

9. Do not merge and do not begin another milestone.