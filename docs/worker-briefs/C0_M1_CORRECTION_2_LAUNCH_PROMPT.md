Continue the same authorized M1 task on repository `barc047-sketch/Mooorf`.

Read and obey:

`docs/worker-briefs/C0_M1_CORRECTION_2_INSPECTOR_VOID_MEMBRANE_COLOUR.md`

from:

`origin/docs/mooorf-ai-team-operating-protocol`

Critical requirements:

1. Work only on `feature/c0-m1-inspector-layer-editing-recovery`.
2. Verify current reviewed head exactly `484ddeb859fcb18256a6acf7b7287e2be917aedc` before editing.
3. Keep `main` unchanged at `c4600472ea76f651800c19b91cf8f67954ca992e`.
4. Fix the widget lifecycle generically: clicking a launcher must open, focus, unminimize and restore the widget to a reachable viewport position. The Dock and Rail `i` buttons must show the full Inspector body in one click.
5. Remove the Void’s unconditional hard-coded inner circle from live and export paths. Preserve outer fill/edge and all subtractive geometry semantics. Optional Inner Echo remains M2, default OFF.
6. Preserve the current Membrane Cell-derived gradient as the default `Cell Gradient` mode.
7. Add a truthful `Solid` Membrane mode with Black, Ink, MOOORF Red, Charcoal and Custom choices. Solid mode must not inherit Cell colour patches.
8. Use canonical persisted state and existing registry/material seams; do not create a duplicate UI-only material catalogue or full Material Browser.
9. Record M2 ownership precisely: Field Edge Softness and independent Membrane Edge Softness are separate advanced controls inside the same Inspector.
10. Add executable widget-lifecycle, renderer, history, persistence and export tests. Source-text checks alone are insufficient.
11. Run affected tests, typecheck, exact correction diff check, exactly one final production build, and 1440×900 plus 1280×800 QA.
12. Update the implementation report, ownership map and `status/codex`.
13. Push one corrected fixed head, return to `WAITING_REVIEW`, and stop.
14. Do not merge and do not start M2.