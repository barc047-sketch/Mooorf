Execute authorized research task `AG3` for repository `barc047-sketch/Mooorf`.

Read and obey the complete contract first:

`docs/worker-briefs/AG3_ANTIGRAVITY_VISUAL_SYSTEMS_RESEARCH_ATLAS.md`

from:

`origin/docs/mooorf-ai-team-operating-protocol`

Critical rules:

1. Work only on:
   `research/ag3-visual-systems-atlas`

2. Verify exact base:
   `main@bd160b8c615cdafbbdd8a76332be46b69a6e765e`

3. This is research-only. Do not change production code, `main`, Codex’s M2 branch, package/configuration files or `status/codex`.

4. Update `status/antigravity` to `RUNNING` before research.

5. Produce only the six required files under `docs/research/`:
   - `AG3_SHADER_CONTROL_ATLAS.md`
   - `AG3_PALETTE_AND_MATERIAL_CURATION.md`
   - `AG3_M2_SYMBOL_VISUAL_QA_SHORTLIST.md`
   - `AG3_VISUAL_PRESET_RECIPES.md`
   - `AG3_VISUAL_SYSTEM_MANIFEST.json`
   - `AG3_HANDOFF.md`

6. Shader work must map real canonical owners, recommended safe ranges, interactions, performance tiers, export behavior and runtime power gates. Do not implement shader code or invent fake controls.

7. Palette work must curate the existing system into a small premium production shortlist. Do not add random colour volume or change canonical production values.

8. Symbol work must reconcile the existing 77 baseline, 59 verified additions, 14 aliases, 8 full custom briefs, 12 pending custom gaps and 5 rejects. Define the smallest strong staged M2 catalogue; do not turn the projected ceiling into automatic scope.

9. Keep UI command icons separate from user-placeable drawable symbols.

10. Preset recipes are specifications only. Do not create production preset code.

11. Use durable repository-relative evidence and primary/official external sources where needed. Do not include local machine paths or unknown-license assets.

12. Validate the JSON manifest, run:
    `git diff --check bd160b8c615cdafbbdd8a76332be46b69a6e765e...HEAD`

13. Prove the research branch changes only `docs/research/`.

14. Push one fixed research head, update `status/antigravity` to `WAITING_REVIEW` with the exact SHA, and stop.

Do not merge.
Do not begin production implementation.
Do not begin another Antigravity task.
