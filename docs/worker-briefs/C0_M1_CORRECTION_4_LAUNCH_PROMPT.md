# C0 M1 Correction 4 Launch Prompt

Continue the same authorized M1 task for repository `barc047-sketch/Mooorf`.

Read and obey:

`docs/worker-briefs/C0_M1_CORRECTION_4_GLOBAL_I_SHORTCUT.md`

from:

`origin/docs/mooorf-ai-team-operating-protocol`

Critical rules:

1. Work only on `feature/c0-m1-inspector-layer-editing-recovery`.
2. Verify current reviewed head exactly `f2d6f99c34257a04e42d4dd6aae2f9b59898d8f6`.
3. Keep `main@c4600472ea76f651800c19b91cf8f67954ca992e` unchanged.
4. Implement the approved global keyboard `I` shortcut through the canonical widget lifecycle.
5. The shortcut must work from Canvas and Table when focus is not inside an editable control.
6. Closed/minimized/background/off-screen Inspector states must open, expand, focus and reveal. A fully visible frontmost Inspector may close on a second `I`, matching the approved prototype toggle behaviour.
7. Never intercept typing in input, textarea, select, contenteditable, inline Cell editor or other editable roles.
8. Ignore Ctrl/Meta/Alt+I and repeated keydown events.
9. Preserve Escape and all Correction 3 pointer/Enter/Space launcher behaviour.
10. Add real built-browser tests using physical keyboard input; direct store/helper calls are insufficient.
11. Run focused tests, typecheck, exact diff check, exactly one final build and both viewport QA passes.
12. Update the M1 report, ownership map and `status/codex`, push one fixed head, return to `WAITING_REVIEW`, and stop.
13. Do not merge and do not start M2.