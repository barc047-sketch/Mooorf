Continue the same authorized M1 task on repository `barc047-sketch/Mooorf`.

Read and obey the latest correction contract:

`docs/worker-briefs/C0_M1_CORRECTION_1_CANCEL_AND_STATUS_TRUTH.md`

from:

`origin/docs/mooorf-ai-team-operating-protocol`

Critical requirements:

1. Work only on `feature/c0-m1-inspector-layer-editing-recovery`.
2. Verify current reviewed head `e9bd67e8c7778dccdd4afb4c1508db0792e70b21` before editing.
3. Keep `main` unchanged at `c4600472ea76f651800c19b91cf8f67954ca992e`.
4. There is exactly one Inspector. M1 keeps Content and Appearance; M2 later adds Symbol as a tab inside this same Inspector.
5. Restructure Appearance to three primary user-facing families while preserving six canonical internal targets:
   - Cell → Surface, Boundary, Core/nucleus
   - Membrane → Field, Edge
   - Void → Fill, Edge
6. Fix Escape cancellation and truthful Inspector status.
7. Restore the bounded bottom-bar functional baseline.
8. Make the visible `i`/Inspector launcher and direct active-family Detail launcher work in one click.
9. Repair and prove Cell Surface and Boundary controls in Classic and Organism.
10. Implement truthful Organism rendering for all six Boundary styles, including real dash-dot, using the existing pointer-transparent overlay.
11. Void currently works according to the Owner. Regression-protect it; repair only a reproduced defect.
12. Reproduce and fix white centre dots remaining when Core is off. The legacy WebGL embedded-dot amount is already zero, so trace the Canvas2D Core/default/local/preview/debug path precisely.
13. Preserve the approved Inspector prototype map: advanced instruments and Symbol tab in M2; Material Browser in M4. Do not create a separate Symbol Inspector.
14. Add executable behavioural and renderer tests; source-string tests alone are insufficient.
15. Run focused tests, affected M1/C0.4F-A contracts, typecheck, exact correction diff check, exactly one final build, and deterministic 1440×900 plus 1280×800 QA.
16. Update M1 report and ownership map with an Owner-observed issue disposition table.
17. Push one corrected fixed head, update `status/codex` to `WAITING_REVIEW`, and stop.
18. Do not merge and do not start M2.