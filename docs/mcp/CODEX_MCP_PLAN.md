# Codex MCP Plan

Required now: none.

Useful later:

1. Playwright — browser and local-preview testing only.
2. Context7 or equivalent — library documentation lookup only.
3. 21st.dev Magic — visual components adapted to ZONUERT tokens; no graph/store access.
4. GitHub — issues and pull requests after a GitHub workflow is active.
5. Figma — design context when a Figma reference exists.

Current Codex MCP list exposes the bundled browser-support tools (`computer-use` and `node_repl`). No project MCP was added.

Any MCP requiring authentication or an API key remains optional and must not block work. Never grant an unknown MCP broad file-write access or allow an MCP to touch secrets, central graph/store/export schemas, backend, auth, payment, or cloud behavior.
